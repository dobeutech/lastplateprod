interface:
  display_name: "Lastplateprod"
  short_description: "Repo-specific patterns and workflows for lastplateprod"
  default_prompt: "Use the lastplateprod repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true